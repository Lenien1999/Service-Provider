import 'package:flutter/material.dart';

appstyle(Color color, FontWeight fw, double size, String ff) {
  return TextStyle(
    fontSize: size,
    fontWeight: fw,
    color: color,
    fontFamily: ff,
  );
}

const Color primaryClr = Color.fromRGBO(95, 96, 185, 1);
const Color titleClr = Color.fromRGBO(108, 117, 125, 1);
const Color headingClr = Color.fromRGBO(28, 31, 52, 1);
const Color tilegClr = Color.fromRGBO(246, 247, 249,1 );
